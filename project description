# 1

use tensorflow : model components  and  deep learnings layers 
tensorflow  graph : key component bec tensowflow works using a graph it compiles entire neural network into a graph 
model used : siamese neural network (one shot image recog /classification) pass 2 images and compare if its same 
 


# 2: 
data used : lfw from kaggle 
unpack labelled faces in the wild 
collect anchor image # using opencv webcam 
collect positive image # using opencv webcam
negative image # using labeled faces in wild data set open datset 




                                SAME MODEL 
anchor/  inp image (webcam) -> model encoding (converting into px to embeddings)   
                                                                     \
                                                                     -> distance layer  -> output 1 (verified)
positive img                -> model encoding                        /







anchor/  inp image (webcam) -> model encoding (converting into px to embeddings)   
                                                                     \
                                                                     -> distance layer(not same) -> output 0 ( not verified)
Negative img                -> model encoding                        /






# 3 
1. preprocess images 
2. create +pos and -neg samples////// pos+anc -> model ////// neg+anc->model 
3. load data into TF data loader (tensorflow data loader : allows to build pipelines makes dl model easier )




#4
start by building data translator = embedding layer to form feature mapping pipeline  
    pass image -> emb layer( convert raw img to data  representation) -> model 
create L1 distance layer 2 imgs -> comapre using l1 distance layer for similarity -> T/F
compile simaese network 



# 5 MODEL TRAINING 
define loss function to cal error between actual and predicted 
define optimiser to back propogate  the updated weights through neural nwk 
build custom training step (to handle different types of inputs for advanced neural networks) for batches

basic flow for training one batch :
1. make prediction 
2. calculate loss 
3. derive gradients 
4. calculate new weights and apply 


create a training loop 
train the model 





# 6 TEST THE MODEL
>> make predictions : then convert confidence matriix to 0 or 1 vals using 0.5 threshold 
>> Evaluate model performance using f1 score and recall
>> save the model for deployment





# 7 Real time predictions 
>> set up images that can be used  for verification :
    application_data {this will have 2 folders} - positive verif images , negative verif images 

>> build verify fxn : loop through all images and compare it to input from webcam (similar to face id on iphones)




WEBCAM (using opencv)  -> retrieve input image -> folder/positives(50 images) {this will perform 50 different verification cycle} 1 full cyclwe = 50 predictions



            input image 
                    \
                     -> distance layer(neural network) -> [0-1] ->use detection threshold fxn -> output 0 or 1 
                   /
            positive images 

verification threshold fxn: 0.5 {x > 0.5 -> 1 else 0 }
implement detection threshold  30 images pass 0.5 threshold = 30/50
                    




>> perform recognition in real time :using opencv to perform real-time facial verification 



webc